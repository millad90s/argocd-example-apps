###kubernetes service file for nginx deployment
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: nginx-deploy
spec:
  selector:
    app: nginx-deploy
  ports:
    - protocol: TCP   
      port: 8080
      targetPort: 80
  type: ClusterIP
